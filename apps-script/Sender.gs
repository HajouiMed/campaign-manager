/**
 * Sender.gs
 * Communication with Node.js SMTP connector.
 * Sends job batches, receives results.
 * Milestone 10
 */

const Sender = {
  sendBatch: function(jobs) {
    // TODO: Implement in Milestone 10
    // POST to Node.js /send endpoint
    // Update queue status based on results
  }
};
